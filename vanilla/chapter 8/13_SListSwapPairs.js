// [] SList: Swap Pairs
// Given a singly linked list, rearrange the nodes so that successive pairs of nodes are swapped in sequence. If list has an odd number of nodes, the final node (not part of a pair) should be unchanged. 

// For mySList a=>b=>c, mySList.swapPairs() should change the mySList object to b=>a=>c. For mySList a=>b=>c=>d, mySList.swapPairs() should change it to b=>a=>d=>c.

